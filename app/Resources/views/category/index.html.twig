<table class="table table-bordered">
    <thead>
        <tr>
            <th>Name</th>
        </tr>
    </thead>
    <tbody>
    {% for category in categories %}
        <tr>
            <td><a href="{{ path('category_edit', { 'id': category.id }) }}">{{ category.name }}</a></td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<a href="{{ path('category_new', { quiz: categories.first.quiz.id}) }}">Create a new entry</a>
