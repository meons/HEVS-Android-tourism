{% extends 'base.html.twig' %}

{% block header %}{{ 'quiz.add.title'|trans }}{% endblock %}
{% block header_desc %}{{ 'quiz.add.desc'|trans }}{% endblock %}

{% block body %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('quiz_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
