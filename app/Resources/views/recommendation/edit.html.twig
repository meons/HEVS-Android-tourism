{% extends 'base.html.twig' %}

{% block header %}{{ 'recommendation.edit.title'|trans }}{% endblock %}
{% block header_desc %}{{ 'recommendation.edit.desc'|trans }}{% endblock %}

{% block body %}
    {{ form_start(delete_form) }}
        <input type="submit" value="{{ 'delete'|trans }}" class="btn btn-danger">
    {{ form_end(delete_form) }}

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="{{ 'save'|trans }}" class="btn btn-success" />
    {{ form_end(edit_form) }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {% include 'recommendation/script_recommendationCriteria.html.twig' %}
{% endblock %}